services:
  - type: web
    name: legal-ai-backend
    env: python
    python:
      version: 3.10.12
    buildCommand: |
      pip install -r requirements.txt
    startCommand: |
      uvicorn api:app --host 0.0.0.0 --port 10000